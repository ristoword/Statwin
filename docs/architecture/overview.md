# Architecture

Monorepo modulare con un backend NestJS centrale.

- `apps/api` — API versionata `/api/v1`
- `apps/web` — prodotto utente
- `apps/admin` — pannello operativo
- `packages/*` — tipi e librerie condivise
- `prisma` — schema e seed strutturali
